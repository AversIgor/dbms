[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "api"
version = "0.1.0"
description = "Read-only HTTP для внешних ИС"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.1",
    "click>=8.1",
    "python-dotenv>=1.0",
]

[project.scripts]
api = "api.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
